# The assignment lives in A1/ as a plain copy (no inner .git), so it is
# tracked here.  A1/.gitignore (shipped with the handout) still applies.

__pycache__/
*.py[cod]

# LaTeX build artifacts
*.aux
*.out
*.synctex.gz
*.fls
*.fdb_latexmk

# LaTeX build scratch
*.synctex(busy)
*.synctex
*.fls
*.bbl
*.blg
*.toc
