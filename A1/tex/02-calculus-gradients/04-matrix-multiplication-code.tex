\item \points{2d} \textbf{Matrix Gradient Implementation}

Implement a function that computes the gradients $\frac{\partial s}{\partial A}$ and $\frac{\partial s}{\partial B}$ for the scalar $s = \sum_{i,j} (AB)_{i,j}$ using NumPy operations.

\textbf{Hint: }Feel free to use the \texttt{np.ones} or \texttt{np.repeat} functions to create the gradient matrices.

\expect{Implement \texttt{matrix\_grad(A, B)} in \texttt{submission.py} that returns a tuple \texttt{(grad\_A, grad\_B)} where each gradient matrix has the same shape as the corresponding input matrix.}