\item \points{2b} \textbf{Gradient Warmup Implementation}
Given a vector $\mathbf w$ and constants $\mathbf c$, compute the gradient $\nabla f(\mathbf w)$ where $f(\mathbf w)=\sum_{i=1}^d (w_i-c_i)^2$.

\expect{Implement \texttt{gradient\_warmup(w, c)} in \texttt{submission.py} that takes vectors \texttt{w} and \texttt{c} and returns the gradient vector.}