\item \points{3c} \textbf{Gradient Descent for a 1D Quadratic}

Implement \texttt{gradient\_descent\_quadratic(x, w, theta0, lr, num\_steps)} in \texttt{submission.py} to minimize the scalar objective
$f(\theta) = \sum_{i=1}^n w_i (\theta - x_i)^2$, where $\theta\in\mathbb{R}$ (recall problem 3a).
The function should return the final scalar iterate after \texttt{num\_steps} gradient steps.

\expect{Implementation of gradient descent to minimize the quadratic function from problem 3a. Your function should perform \texttt{num\_steps} iterations of gradient descent starting from \texttt{theta0} with learning rate \texttt{lr}.}