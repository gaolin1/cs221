\item \points{4b}

Implement \texttt{m\_step(BayesianNetwork, data)} which takes in the dataset from the \texttt{e\_step} function and updates the parameters of the Bayesian network using maximum likelihood estimation.

\expect{
An implementation of the \texttt{m\_step} function in \texttt{submission.py}. Your code should use your earlier implementations of \texttt{accumulate\_assignment} and the provided \texttt{init\_zero\_conditional\_probability\_tables} and \texttt{normalize\_counts}.
}