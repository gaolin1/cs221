\item \points{4c}

Implement \texttt{em\_learn(BayesianNetwork, data, num\_iterations)} which runs the EM algorithm for a given number of iterations.

\expect{
    An implementation of the \texttt{em\_learn} function in \texttt{submission.py}.
}