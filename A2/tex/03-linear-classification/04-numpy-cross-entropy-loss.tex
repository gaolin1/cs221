\item \points{3d} \textbf{NumPy Cross-Entropy Loss}

Implement \texttt{numpy\_cross\_entropy\_loss(predictions, targets, epsilon)}, which returns a floating point number measuring how far the predicted probabilities are from the true one-hot labels, on average.