\item \points{3e} \textbf{NumPy Gradient Computation}

Implement \texttt{numpy\_compute\_gradients(features, predictions, targets)}, which returns the gradients for weight and bias, respectively. This is later used to update model weights using backpropagation. 