\item \points{3a} \textbf{Build vocabulary}

Implement \texttt{build\_vocabulary(examples)}, which builds a vocabulary using
words seen in the training examples. Your implementation should populate the \texttt{Vocabulary} class provided in \texttt{util.py}, which will
be used in part b to build a sparse feature representation for any input text.

\expect{Implement \texttt{build\_vocabulary(examples)} using the \texttt{Vocabulary} class from \texttt{util.py}.}