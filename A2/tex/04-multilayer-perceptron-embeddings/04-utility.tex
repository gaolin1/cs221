\item \points{4d} \textbf{Utility functions}

Implement the helper functions tailored to our three-class prediction task.

\begin{itemize}
    \item \texttt{torch\_softmax(logits)}: Convert logits to probabilities
    \item \texttt{torch\_cross\_entropy\_loss(predictions, targets)}: A custom function that computes the average cross entropy loss of each training batch
    \item \texttt{update\_parameter(param, grad, lr)}: Manual gradient descent update
\end{itemize}